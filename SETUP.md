# Инструкция по запуску админ-панели

## ✅ Что уже сделано

1. **Phase 1: Setup** - Завершено
   - Структура проекта создана
   - Конфигурационные файлы настроены (Vite, TypeScript, Tailwind, ESLint)
   - Базовая структура FSD создана

2. **Phase 2: Foundational** - Завершено
   - Базовые UI компоненты (Button, Input, Card, Table, Modal, Badge, Select, Textarea)
   - API клиент с обработкой токенов
   - React Query провайдер
   - React Router настроен
   - Layout с Header и Sidebar
   - Утилиты (cn, token management, toast)

## 🚀 Следующие шаги

### 1. Установите зависимости

```bash
cd admin-panel
npm install
```

### 2. Создайте файл .env

```bash
cp .env.example .env
```

Или создайте `.env` вручную:
```
VITE_API_URL=https://volunteers-backend-production.up.railway.app
```

### 3. Запустите dev сервер

```bash
npm run dev
```

Приложение будет доступно на http://localhost:3001

## 📋 Что дальше

Следующий этап - **Phase 3: User Story 1 - Авторизация админа**

Нужно реализовать:
- Типы для авторизации
- API методы авторизации
- Хуки для логина/регистрации
- Страницу авторизации
- Формы логина и регистрации
- Защиту роутов

## 📁 Структура проекта

```
admin-panel/
├── src/
│   ├── app/              # Инициализация, роутинг, провайдеры
│   ├── pages/            # Страницы приложения
│   ├── widgets/          # Виджеты (Layout)
│   ├── features/         # Бизнес-фичи (пока пусто)
│   ├── entities/         # Бизнес-сущности (пока пусто)
│   └── shared/           # Общие компоненты и утилиты
│       ├── ui/           # UI компоненты
│       ├── api/          # API клиент
│       └── lib/          # Утилиты
```

## 🎨 UI Компоненты готовы

- ✅ Button (primary, secondary, danger, ghost)
- ✅ Input (с label и error)
- ✅ Textarea (с label и error)
- ✅ Select (с label и error)
- ✅ Card (с title и actions)
- ✅ Modal (разных размеров)
- ✅ Table (TableHead, TableBody, TableRow, TableCell)
- ✅ Badge (разных вариантов)

Все компоненты используют Tailwind CSS и готовы к использованию!
