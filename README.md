# Админ-панель - Волонтерское приложение

Админ-панель для управления волонтерским приложением.

## 🚀 Быстрый старт

```bash
# Установка зависимостей
npm install

# Создание .env файла
cp .env.example .env

# Запуск dev сервера
npm run dev
```

Приложение будет доступно на http://localhost:3001

## 📦 Технологии

- **React 18** - UI библиотека
- **TypeScript** - Типизация
- **Vite** - Сборщик
- **Tailwind CSS** - Стилизация
- **React Query** - Управление состоянием сервера
- **React Router** - Роутинг
- **Sonner** - Toast уведомления

## 🌐 Локализация

Админ-панель поддерживает **три языка интерфейса**: русский (`ru`), английский (`en`) и иврит (`he`).

- Все тексты UI берутся из i18n-словарей в `src/shared/lib/i18n/locales/`.
- Корневой провайдер `I18nProvider` подключается в `src/app/index.tsx` и оборачивает всё приложение.
- Текущий язык хранится в локальном хранилище и задаётся хуком `useI18n`.
- Переключение языка влияет на:
  - подписи меню, заголовки страниц и таблиц;
  - все формы, уведомления и ошибки;
  - направление интерфейса (`dir="rtl"` для иврита и `dir="ltr"` для других языков).

Рекомендуется при изменении текстов в UI сначала добавлять/обновлять ключи переводов в `src/shared/lib/i18n/locales/{ru,en,he}.json`, а затем использовать их через `useI18n().t(key)`.

**Соглашение по i18n**: В админ‑панели запрещено добавлять новые UI‑строки без ключа i18n. Все видимые пользователю тексты (заголовки, кнопки, подписи, сообщения об ошибках, тосты) должны браться из словарей и выводиться через `t('key')`. Исключения (например, логи или отладочные подписи) допустимы только при явной договорённости и не должны попадать в прод.

## 🏗️ Архитектура

Проект использует **Feature-Sliced Design (FSD)** архитектуру:

- `app/` - Инициализация приложения, роутинг, провайдеры
- `pages/` - Полные страницы приложения
- `widgets/` - Крупные самодостаточные блоки функциональности
- `features/` - Переиспользуемые бизнес-фичи
- `entities/` - Бизнес-сущности (данные и их отображение)
- `shared/` - Переиспользуемые компоненты без бизнес-логики

## ✅ Реализовано

### Phase 1: Setup ✅
- Структура проекта
- Конфигурация (Vite, TypeScript, Tailwind, ESLint)
- Базовая структура FSD

### Phase 2: Foundational ✅
- Базовые UI компоненты (Button, Input, Card, Table, Modal, Badge, Select, Textarea)
- API клиент с автоматическим обновлением токенов
- React Query провайдер
- React Router
- Layout с Header и Sidebar
- Утилиты (cn, token management, toast)

## 📋 TODO

### Phase 3: Авторизация (в процессе)
- [ ] Типы для авторизации
- [ ] API методы авторизации
- [ ] Хуки для логина/регистрации
- [ ] Страница авторизации
- [ ] Защита роутов

### Phase 4-7: Основной функционал
- [ ] Управление программами
- [ ] Управление тасками
- [ ] Управление пользователями
- [ ] Навыки и категории

## 🔧 Переменные окружения

Создайте файл `.env`:

```env
VITE_API_URL=https://volunteers-backend-production.up.railway.app
```

## 📝 Скрипты

```bash
npm run dev      # Запуск dev сервера
npm run build    # Сборка для продакшена
npm run preview  # Просмотр production сборки
npm run lint     # Проверка кода линтером
```

## 📚 Документация

Подробная инструкция по настройке: [SETUP.md](./SETUP.md)

## 🎨 UI Компоненты

Все компоненты готовы к использованию:
- Button (4 варианта: primary, secondary, danger, ghost)
- Input (с label и error)
- Textarea (с label и error)
- Select (с label и error)
- Card (с title и actions)
- Modal (4 размера: sm, md, lg, xl)
- Table (полный набор компонентов)
- Badge (5 вариантов: default, success, warning, danger, info)
